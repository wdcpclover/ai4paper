{
  "name": "ai4paper",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "package:codex:0.1": "npm run build && zip -qr ai4paper-codex-0.1.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.2": "npm run build && zip -qr ai4paper-codex-0.2.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.3": "npm run build && zip -qr ai4paper-codex-0.3.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.4": "npm run build && zip -qr ai4paper-codex-0.4.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.5": "npm run build && zip -qr ai4paper-codex-0.5.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.6": "npm run build && zip -qr ai4paper-codex-0.6.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.7": "npm run build && zip -qr ai4paper-codex-0.7.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'",
    "package:codex:0.8": "npm run build && zip -qr ai4paper-codex-0.8.xpi manifest.json bootstrap.js prefs.js chrome locale -x '*.DS_Store'"
  }
}
